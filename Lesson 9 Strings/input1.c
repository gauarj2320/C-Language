//Taking input from user without considering whitespaces
#include<stdio.h>
int main()
{
    char str[50];
    printf("Entet the string:");
    scanf("%s",str);
    printf("Entered string is %s",str);
    return 0;
}