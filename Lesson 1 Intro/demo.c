#include <stdio.h> // preprocessor directive to include standard input output file --> contain the working / definition of printf function
int main() // entry point
{
    printf("Hi Arjun"); // print statemetn
    return 0;
}